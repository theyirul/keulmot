# 배포되는 것은 html 3개 + style.css + photos.js + assets/ 뿐이다.
# 나머지는 전부 로컬 전용.

# 내부 메모 — 공개 안 함
progress.md

# 원본 재료 — 공개 안 함 (assets/ 의 리사이즈본만 배포)
클못시공 사례 사진/
클못 시공 사례.xlsx

# 생성기·로컬 도구 — 공개 안 함
build.py
preview.py
tuner.py
미리보기.command

# 지난 안·내부 문서·레퍼런스 원본 — 공개 안 함
# (leibal-home.html 처럼 남의 자산도 여기 들어 있다)
archive/

# 작업 중 임시 파일
scratch-csv/
__diag.html
preview-align-*.html
.DS_Store
